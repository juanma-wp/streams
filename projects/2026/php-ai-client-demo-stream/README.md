# PHP AI Client Test Plugin

A minimal WordPress plugin to test the `wordpress/wp-ai-client` package.

## Installation

1. Run `composer install` to install dependencies
2. Copy this plugin directory to your WordPress `wp-content/plugins/` folder
3. Activate the plugin in WordPress admin
4. Enter the credential from the WP Admin page
5. Call the functions from a plugin or from `wp shell`

